apiVersion: v1
kind: Service
metadata:
  name: {{ template "restic-metrics.fullname" . }}
  labels:
    app: {{ template "restic-metrics.name" . }}
    chart: {{ template "restic-metrics.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "restic-metrics.name" . }}
    release: {{ .Release.Name }}
